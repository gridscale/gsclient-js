export declare const $MarketplaceApplicationSetup: {
    properties: {
        cores: {
            type: string;
            maximum: number;
            minimum: number;
        };
        memory: {
            type: string;
            maximum: number;
            minimum: number;
        };
        capacity: {
            type: string;
            maximum: number;
            minimum: number;
        };
    };
};
